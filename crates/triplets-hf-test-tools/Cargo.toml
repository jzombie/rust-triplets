[package]
name = "triplets-hf-test-tools"
version = "0.1.0"
edition = "2024"
description = "Shared mock HTTP servers and test utilities for triplets HuggingFace integration tests."
license = "MIT OR Apache-2.0"

[dependencies]
serde_json = "1.0.149"
