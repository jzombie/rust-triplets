# Plan: Fix PR #157 — Dependency Audit Failure & Test Coverage Gaps

## Problem

PR #157 (`fix/hf-json-array-parsing`) fails CI with:
1. `cargo-audit` error: `h2` v0.4.13 has vulnerability RUSTSEC-2026-0258
2. Missing unit tests for new functionality

## Changes

### 1. Update `h2` dependency (Cargo.lock)

`cargo update -p h2` upgrades from v0.4.13 → v0.4.19, resolving the audit failure. Already done locally; just needs the lock file committed.

### 2. Add unit tests for `extract_next_link_url`

**File:** `crates/triplets-hf-source/src/download/download_tests.rs`

Tests to add:
- Absolute URL with `rel="next"` — returns resolved URL
- Relative URL resolved against request_url — returns correct absolute URL
- Multiple `Link` headers (split across lines) — finds `rel="next"` on second line
- Malformed segment (no angle brackets) — continues to next segment
- No `Link` header — returns `None`
- `rel="prev"` only — returns `None`

### 3. Add unit tests for `peek_first_non_whitespace`

**File:** `crates/triplets-hf-source/src/rows/rows_tests.rs`

Tests to add:
- Input starts with `[` — returns `Some(b'[')`
- Input has leading whitespace then `{` — returns `Some(b'{')`
- Empty input — returns `None`
- Only whitespace — returns `None`

### 4. Add unit tests for JSON array streaming

**File:** `crates/triplets-hf-source/src/rows/rows_tests.rs`

Tests to add:
- Empty array `[]` — produces 0 rows
- Single object `[{...}]` — produces 1 row
- Multiple objects — produces correct count
- Scalar strings `["a", "b"]` — normalized to `{"text": "..."}` objects
- Mixed scalars and objects — all processed correctly
- Trailing partial batch (not multiple of 1024) — all rows flushed

### 5. Add unit test for weight map cleanliness on failure

**File:** `crates/triplets-hf-source/src/builder/builder_tests.rs`

Test to add:
- Source with valid URI but failing init — `BuildResult.failures` contains entry, `BuildResult.weights` does NOT contain the source ID, `BuildResult.sources` excludes it

## Verification

1. `cargo audit` — no vulnerabilities
2. `cargo test -p triplets-hf-source` — all tests pass including new ones
3. `cargo clippy --workspace --all-targets --all-features -- -D warnings` — clean
